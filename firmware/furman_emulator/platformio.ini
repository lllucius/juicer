[env:esp32dev]
platform = espressif32
board    = esp32dev
framework = arduino

; 9600 baud matches the Juicer SerialTransport default.
; USB-CDC adapters ignore this value from the host side, so
; any port speed set by the host works transparently over USB.
monitor_speed = 9600
